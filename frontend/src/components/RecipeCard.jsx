import {useState} from "react";
import {RecipeDetailModal} from "./RecipeDetailModal";
export function RecipeCard({recipe,headers,apiUrl="",formatPrice,onSource}){const[open,setOpen]=useState(false);return <><article className="flex min-h-64 flex-col rounded-3xl bg-white p-5 shadow-sm"><div className="flex justify-between gap-2"><span className="rounded-full bg-mint px-3 py-1 text-xs font-bold text-forest">{recipe.meal_type}</span><span className="text-xs font-bold text-sage">{recipe.pantry_match_percent}% pantry</span></div><h3 className="mt-5 text-2xl font-bold">{recipe.title}</h3><p className="mt-2 text-sm text-ink/60">{recipe.missing_ingredients?.length?recipe.missing_ingredients.map(i=>i.ingredient_name).join(", "):"Everything is in your pantry."}</p><div className="mt-auto pt-5"><div className="flex flex-wrap gap-2 text-xs font-semibold"><span className="rounded-lg bg-cream px-2 py-1">◷ {recipe.prep_time} min</span><span className="rounded-lg bg-cream px-2 py-1">P {recipe.protein_g}g · C {recipe.carbs_g}g · F {recipe.fat_g}g</span></div><div className="mt-3 flex items-center justify-between"><span className="rounded-full bg-lime/60 px-3 py-2 text-sm font-bold text-forest">Dish cost {formatPrice(recipe.estimated_missing_cost)}</span><button onClick={()=>setOpen(true)} className="rounded-xl bg-forest px-4 py-2 text-sm font-bold text-white">View & Source</button></div></div></article>{open&&<RecipeDetailModal recipe={recipe} headers={headers} apiUrl={apiUrl} formatPrice={formatPrice} onClose={()=>setOpen(false)}/>}</>}
